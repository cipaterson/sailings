<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks cleartext HTTP by default (its equivalent of iOS App Transport
  Security). This permits it for local development only: 10.0.2.2 is the
  emulator's alias for the host machine. Everything else (i.e. production) must
  be HTTPS.

  To test on a *physical device* against a dev server, add your Mac's LAN IP as
  its own <domain>, e.g.:
      <domain includeSubdomains="false">192.168.1.42</domain>
  (an exact address — this file matches hostnames/IPs, not CIDR ranges) and set
  the same address in Config.kt.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
