<h1><%= @sailing.name %></h1>

<%= render "form", assignment: @assignment %>

<%= link_to "Cancel", sailings_path %>
<%= button_to "Delete", @assignment, method: :delete, data: { turbo_confirm: "Are you sure?" } %>
