<h1>Members</h1>

<%= link_to "New Member", new_user_path if can?("office_staff") %>

<table>
  <thead>
    <tr>
      <th><%= sort_link "Name", "last_name" %></th>
      <th><%= sort_link "Email", "email_address" %></th>
      <th>Mobile Phone</th>
      <th><%= sort_link "Membership Type", "membership_type" %></th>
      <th>Roles</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= link_to user.full_name, user %></td>
        <td><%= user.email_address %></td>
        <td><%= user.mobile_phone %></td>
        <td><%= user.membership_type %></td>
        <td><%= user.roles.join(", ") %></td>
      </tr>
    <% end %>
  </tbody>
</table>
